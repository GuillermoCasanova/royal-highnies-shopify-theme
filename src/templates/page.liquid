
<article class="section--main">
	<div class="container-fluid">
		<div class="row">
			<div class="col-sm-12 col-lg-10">
				<section>
					<h1>{{ page.title }}</h1>
						
					<div class="rte">
					  {{ page.content }}
					</div>
				</section>
			</div>
		</div>
	</div>
</article>
